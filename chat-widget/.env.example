# Copy to .env and set your key locally (do NOT commit real keys)
OPENAI_API_KEY=sk-your_key_here
OPENAI_MODEL=gpt-4o-mini
PORT=8787
# Proxy authentication token (server only). Clients should use short-lived tokens.
PROXY_TOKEN=replace_with_secure_token

